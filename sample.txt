func(var x, var y){
 if(x *2 == y){
   return x == y;
   }else{
     if(x+6  > y ){
        return "x + 6 biggeer than y";
     }
     if(x+6  < y ){
        return "x + 6 lower than y";
     }
     if(x *6 + y <= x*6){
        return x +y > x*y ? x : y;
     }
     if(true){
       return x == y ;
     }

   }
   return x == y ;
}